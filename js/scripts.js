function multiply(number1, number2) {
  return number1 * number2;
  }
  
  const number1 = parseFloat(prompt("Enter the number of gallons:"));

  const number2 = 3.785;
  
  alert(multiply(number1, number2) + " liters");